# 📝 TZ 101Hotels — Список комментариев

## 📌 Описание

Веб-приложение для работы со списком комментариев.

Реализованы:

* добавление комментариев
* удаление комментариев (без перезагрузки страницы)
* сортировка
* постраничный просмотр

---

## ⚙️ Стек технологий

* **PHP 7.4**
* **CodeIgniter 4**
* **MySQL 8**
* **jQuery 3**
* **Bootstrap 4**
* **Docker / Docker Compose**

---

## 🚀 Запуск проекта

### 1. Клонировать репозиторий

```bash
git clone <repo_url>
cd TZ_101hotels_crud
```

### 2. Настроить окружение

```bash
cp .env.example .env
```

### 3. Запустить контейнеры

```bash
docker compose up --build -d
```

### 4. Установить зависимости

```bash
docker exec -it tz_hotels_web composer install
```

### 5. Выполнить миграции

```bash
docker exec -it tz_hotels_web php spark migrate
```

### 6. Открыть в браузере

```text
http://localhost:8080
```

---

## 🧩 Функциональность

### ✅ Комментарии

* добавление комментариев (AJAX, без перезагрузки)
* удаление комментариев (AJAX)
* отображение списка

### ✅ Пагинация

* по **3 комментария на страницу**
* переход между страницами

### ✅ Сортировка

* по `id`
* по дате добавления (`created_at`)
* направления:

    * по возрастанию
    * по убыванию

### ✅ Валидация

* email валидируется:

    * на клиенте
    * на сервере

---

## 📅 Работа с датами

В приложении используются два поля даты:

### `date`

* строковое поле
* вводится пользователем
* формат: `ДД.ММ.ГГГГ ЧЧ:ММ`
* соответствует ТЗ: *"дата выбирается создателем"*

### `created_at`

* служебное поле
* заполняется автоматически
* используется для:

    * сортировки
    * отображения даты добавления

---

## 📌 Важно

Поле `name` в базе данных используется для хранения **email пользователя**.

Это сделано в соответствии с формулировкой ТЗ:

> `name` — почта создателя

---

## 🗂️ Структура проекта

```
app/
 ├── Controllers/
 ├── Models/
 ├── Views/
 └── Database/
     └── Migrations/

public/
 └── assets/
     ├── css/
     └── js/

docker-compose.yml
Dockerfile
```

---

## 🔌 API (используется через AJAX)

| Метод  | URL              | Описание             |
| ------ | ---------------- | -------------------- |
| GET    | `/`              | список комментариев  |
| POST   | `/comments`      | добавить комментарий |
| DELETE | `/comments/{id}` | удалить комментарий  |

---

## 🧪 Примечания

* Приложение настроено в режиме `production` (debug toolbar отключён)
* Для разработки можно включить режим отладки:

```ini
CI_ENVIRONMENT = development
```

---

## ✅ Соответствие ТЗ

* ✔ добавление и удаление комментариев (AJAX)
* ✔ пагинация (3 на страницу)
* ✔ сортировка по id и дате
* ✔ выбор направления сортировки
* ✔ валидация email
* ✔ адаптивная верстка
* ✔ использование jQuery

---

## 📎 Автор

Тестовое задание для 101Hotels
